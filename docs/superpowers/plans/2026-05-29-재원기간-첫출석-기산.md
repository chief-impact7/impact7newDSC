# 재원기간 기산일을 "첫 출석일"로 변경 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 재원기간(`deriveTenure`)의 기산일을 history '신규' 이벤트 대신 "현재 재원 구간의 첫 실제 출석일"로 바꿔, 미등원 학생이 재원 N일로 잡히지 않게 한다.

**Architecture:** `@impact7/shared`의 `deriveTenure`를 순수함수로 유지하되 `attendances`(`{date,status}` 배열)를 받아 내부에서 `isAttendedStatus`로 필터링해 첫 출석일을 계산. 양 앱(impact7DB·impact7newDSC)은 `daily_records`를 한 번 더 읽어 주입하고, 표시부에서 미등원이면 `'등원예정'`을 표시.

**Tech Stack:** ESM 순수 JS(shared, `node --test`), Firebase Firestore(웹 클라이언트), Vite 빌드.

**작업 순서(크로스앱):** Task 1(shared) → Task 2(DB앱) → Task 3(newDSC) → Task 4(검증·배포). shared는 GitHub 태그 의존이라 Task 1에서 `v1.11.0` 태그를 push해야 Task 2·3에서 참조 가능.

---

## 파일 구조

| 파일 | 책임 | 작업 |
|------|------|------|
| `~/projects/impact7-shared/history-classifier.js` | `deriveTenure` 시그니처 확장 + `isAttendedStatus` SSoT | Modify |
| `~/projects/impact7-shared/history-classifier.test.js` | deriveTenure 동작 테스트 | Modify |
| `~/projects/impact7-shared/package.json` | 버전 1.11.0 | Modify |
| `~/projects/impact7DB/package.json` | shared 버전 bump | Modify |
| `~/projects/impact7DB/app.js:4313-4360` | `fillTenure` 호출부 + `formatTenure` 표시부 | Modify |
| `~/projects/impact7newDSC/package.json` | shared 버전 bump | Modify |
| `~/projects/impact7newDSC/student-detail.js:82-117` | `fillTenure` + `formatTenure` | Modify |

---

## Task 1: shared — `deriveTenure` 확장 + `isAttendedStatus`

**Files:**
- Modify: `~/projects/impact7-shared/history-classifier.js`
- Test: `~/projects/impact7-shared/history-classifier.test.js`
- Modify: `~/projects/impact7-shared/package.json`

작업 디렉토리: `cd ~/projects/impact7-shared`

- [ ] **Step 1: 기존 deriveTenure 테스트를 새 의미로 갱신 + 신규 테스트 작성 (실패 확인용)**

`history-classifier.test.js`의 기존 deriveTenure 테스트 4개(약 79-118행)를 아래 블록으로 **교체**한다. (기존은 `start`가 신규일이라고 단언 — 새 동작에선 `startEvent`가 그 역할이고 `start`는 첫 출석일이므로 수정 필요.)

```js
import { deriveTenure, isAttendedStatus } from './history-classifier.js';

const gd = (l) => l.date instanceof Date ? l.date : new Date(l.date);
const mkLog = (dateStr, before, after, change_type) => ({
  date: new Date(dateStr + 'T00:00:00+09:00'), before, after, change_type,
});
const att = (date, status) => ({ date, status });

test('deriveTenure: 신규 + 이후 출석 → start=첫 출석일, startEvent=신규일', () => {
  const logs = [mkLog('2026-03-01', '상담', '등원예정', 'UPDATE')];
  const attendances = [att('2026-03-10', '결석'), att('2026-03-12', '출석'), att('2026-03-20', '지각')];
  const { start, end, startEvent } = deriveTenure(logs, gd, attendances);
  assert.strictEqual(startEvent.getTime(), new Date('2026-03-01T00:00:00+09:00').getTime());
  assert.strictEqual(start.getTime(), new Date('2026-03-12T00:00:00+09:00').getTime());
  assert.strictEqual(end, null);
});

test('deriveTenure: 신규 + 출석 0건 → start=null (등원예정), startEvent 세팅', () => {
  const logs = [mkLog('2026-05-20', '상담', '등원예정', 'UPDATE')];
  const attendances = [att('2026-05-25', '결석'), att('2026-05-26', '미확인')];
  const { start, startEvent } = deriveTenure(logs, gd, attendances);
  assert.notStrictEqual(startEvent, null);
  assert.strictEqual(start, null);
});

test('deriveTenure: 재등원 → 마지막 재등원 이후 첫 출석일', () => {
  const logs = [
    mkLog('2025-01-01', '상담', '등원예정', 'UPDATE'),
    mkLog('2025-06-01', '재원', '퇴원', 'WITHDRAW'),
    mkLog('2026-02-01', '퇴원', '재원', 'UPDATE'),
  ];
  // 재등원(2026-02-01) 이전 출석은 무시, 이후 첫 출석이 start
  const attendances = [att('2025-02-10', '출석'), att('2026-02-15', '출석'), att('2026-03-01', '출석')];
  const { start, startEvent } = deriveTenure(logs, gd, attendances);
  assert.strictEqual(startEvent.getTime(), new Date('2026-02-01T00:00:00+09:00').getTime());
  assert.strictEqual(start.getTime(), new Date('2026-02-15T00:00:00+09:00').getTime());
});

test('deriveTenure: 이력 없음 → startEvent=null, start=null', () => {
  const { start, end, startEvent } = deriveTenure([], gd, []);
  assert.strictEqual(startEvent, null);
  assert.strictEqual(start, null);
  assert.strictEqual(end, null);
});

test('deriveTenure: 퇴원이면 end=퇴원일', () => {
  const logs = [
    mkLog('2025-01-01', '상담', '재원', 'UPDATE'),
    mkLog('2025-12-01', '재원', '퇴원', 'WITHDRAW'),
  ];
  const attendances = [att('2025-01-05', '출석')];
  const { start, end } = deriveTenure(logs, gd, attendances);
  assert.strictEqual(start.getTime(), new Date('2025-01-05T00:00:00+09:00').getTime());
  assert.strictEqual(end.getTime(), new Date('2025-12-01T00:00:00+09:00').getTime());
});

test('deriveTenure: attendances 미전달 → start=null (안전)', () => {
  const logs = [mkLog('2026-03-01', '상담', '등원예정', 'UPDATE')];
  const { start, startEvent } = deriveTenure(logs, gd);
  assert.notStrictEqual(startEvent, null);
  assert.strictEqual(start, null);
});

test('isAttendedStatus: 출석/지각/조퇴만 true', () => {
  assert.strictEqual(isAttendedStatus('출석'), true);
  assert.strictEqual(isAttendedStatus('지각'), true);
  assert.strictEqual(isAttendedStatus('조퇴'), true);
  assert.strictEqual(isAttendedStatus('결석'), false);
  assert.strictEqual(isAttendedStatus('미확인'), false);
  assert.strictEqual(isAttendedStatus(undefined), false);
});
```

> 주의: 기존 테스트에서 `classifyHistory`가 `'2026-03-01'→등원예정`을 `'신규'`로 분류하는지는
> 기존 분류 테스트가 보장한다(`(bS==='' || bS==='상담') && (aS==='등원예정'||aS==='재원')`).

- [ ] **Step 2: 테스트 실행해 실패 확인**

Run: `cd ~/projects/impact7-shared && npm test`
Expected: FAIL — `isAttendedStatus`가 export되지 않았고 `deriveTenure`가 3번째 인자/`startEvent`를 아직 지원하지 않음 (`SyntaxError` 또는 단언 실패).

- [ ] **Step 3: `history-classifier.js` 수정**

파일 끝의 `deriveTenure` 함수를 찾아 아래로 **교체**하고, 그 위에 `ymdSeoul` 헬퍼와 `isAttendedStatus`를 추가한다.

```js
// 출석으로 인정하는 상태 (결석·미확인·등원전 제외) — 양 앱 SSoT.
export function isAttendedStatus(status) {
  return status === '출석' || status === '지각' || status === '조퇴';
}

// Date → KST 'YYYY-MM-DD' (en-CA 로케일이 YYYY-MM-DD 형식을 보장)
function ymdSeoul(date) {
  return new Intl.DateTimeFormat('en-CA', {
    timeZone: 'Asia/Seoul', year: 'numeric', month: '2-digit', day: '2-digit',
  }).format(date);
}

// 재원기간 파생. logs로 현재 재원 구간의 시작 이벤트(startEvent)·종료(end)를 잡고,
// attendances(그 학생의 daily_records {date,status})에서 startEvent 이후 첫 출석일을 start로.
// 반환: { start: Date|null, end: Date|null, startEvent: Date|null }
//   - startEvent=null      → 이력 없음
//   - startEvent≠null, start=null → 미등원(등원예정)
//   - start≠null           → 첫 출석일부터 재원
export function deriveTenure(logs, getDate, attendances) {
  const events = (logs || [])
    .map(l => ({ cat: classifyHistory(l), date: getDate(l) }))
    .filter(e => e.cat && e.date instanceof Date && !isNaN(e.date.getTime()))
    .sort((a, b) => a.date - b.date);
  let startEvent = null, end = null;
  for (const e of events) {
    if (e.cat.label === '신규' || e.cat.label === '재등원') { startEvent = e.date; end = null; }
    else if (e.cat.label === '퇴원') { end = e.date; }
  }
  let start = null;
  if (startEvent && Array.isArray(attendances)) {
    const seStr = ymdSeoul(startEvent);
    const firstAttended = attendances
      .filter(a => a && isAttendedStatus(a.status) && typeof a.date === 'string' && a.date >= seStr)
      .map(a => a.date)
      .sort()[0];
    if (firstAttended) start = new Date(firstAttended + 'T00:00:00+09:00');
  }
  return { start, end, startEvent };
}
```

- [ ] **Step 4: 테스트 실행해 통과 확인**

Run: `cd ~/projects/impact7-shared && npm test`
Expected: PASS — 모든 테스트 통과(기존 classifyHistory 테스트 포함).

- [ ] **Step 5: 버전 bump + 커밋 + 태그 push**

`package.json`의 `"version": "1.10.0"` → `"1.11.0"`.

```bash
cd ~/projects/impact7-shared
git add history-classifier.js history-classifier.test.js package.json
git commit -m "feat: deriveTenure 기산일을 첫 출석일로 (attendances 인자 + isAttendedStatus) v1.11.0"
git tag v1.11.0
git push && git push --tags
```

---

## Task 2: impact7DB — shared bump + 호출부·표시부

**Files:**
- Modify: `~/projects/impact7DB/package.json`
- Modify: `~/projects/impact7DB/app.js` (`fillTenure` ~4313-4338, `formatTenure` ~4341-4360)

작업 디렉토리: `cd ~/projects/impact7DB`

- [ ] **Step 1: shared 버전 bump + 재설치**

`package.json`에서 `"@impact7/shared": "github:chief-impact7/impact7-shared#v1.10.0"` → `#v1.11.0`.

```bash
cd ~/projects/impact7DB
npm install
```
Expected: `node_modules/@impact7/shared/history-classifier.js`에 `isAttendedStatus` export 존재.
확인: `grep -c "export function isAttendedStatus" node_modules/@impact7/shared/history-classifier.js` → `1`

> import 변경 불필요: `isAttendedStatus`는 호출부에서 직접 안 쓰고 shared 내부가 필터하므로 `app.js:7`의 기존 import(`classifyHistory, HISTORY_BADGE, shortAuthor, deriveTenure`)를 그대로 둔다.

- [ ] **Step 2: `fillTenure`에 daily_records 조회 추가 + deriveTenure 3인자 호출**

`app.js`의 `fillTenure`(약 4313-4338) 안에서 history_logs 쿼리와 `deriveTenure` 호출 부분을 아래로 **교체**한다.

```js
        const q = query(
            collection(db, 'history_logs'),
            where('doc_id', '==', studentId),
            orderBy('timestamp', 'desc'),
            limit(200)
        );
        const aq = query(collection(db, 'daily_records'), where('student_id', '==', studentId));
        const [snap, asnap] = await Promise.all([getDocs(q), getDocs(aq)]);
        if (currentStudentId !== studentId) return; // 그 사이 다른 학생 선택됨
        const logs = [];
        snap.forEach(d => logs.push({ id: d.id, ...d.data() }));
        const attendances = [];
        asnap.forEach(d => { const r = d.data(); attendances.push({ date: r.date, status: r.attendance?.status }); });
        const { start, end, startEvent } = deriveTenure(
            logs,
            (l) => l.timestamp?.toDate ? l.timestamp.toDate() : (l.timestamp ? new Date(l.timestamp) : null),
            attendances
        );
        el.textContent = formatTenure(start, end, startEvent, studentData);
```

- [ ] **Step 3: `formatTenure` 시그니처에 `startEvent` 추가 + 등원예정 분기**

`app.js`의 `formatTenure`(약 4341)에서 두 곳만 바꾼다.

1. 시그니처: `function formatTenure(start, end, studentData) {` → `function formatTenure(start, end, startEvent, studentData) {`
2. 맨 앞 줄 `if (!start) return '—';` → 아래로 교체:

```js
    if (!start) return startEvent ? '등원예정' : '—';
```

나머지 본문(`const startStr = formatDate(start);` 이하)은 그대로 둔다.

- [ ] **Step 4: 빌드 검증**

Run: `cd ~/projects/impact7DB && npm run build`
Expected: 빌드 성공(에러 없음).

- [ ] **Step 5: 커밋**

```bash
cd ~/projects/impact7DB
git add package.json package-lock.json app.js
git commit -m "feat: 재원기간 기산일을 첫 출석일로 (@impact7/shared v1.11.0)"
```

---

## Task 3: impact7newDSC — shared bump + 호출부·표시부

**Files:**
- Modify: `~/projects/impact7newDSC/package.json`
- Modify: `~/projects/impact7newDSC/student-detail.js` (`formatTenure` 82-95, `fillTenure` ~100-117)

작업 디렉토리: `cd ~/projects/impact7newDSC`

- [ ] **Step 1: shared 버전 bump + 재설치**

`package.json`에서 `"@impact7/shared": "github:chief-impact7/impact7-shared#v1.10.0"` → `#v1.11.0`.

```bash
cd ~/projects/impact7newDSC
npm install
```
확인: `grep -c "export function isAttendedStatus" node_modules/@impact7/shared/history-classifier.js` → `1`

- [ ] **Step 2: `fillTenure`에 daily_records 조회 추가 + deriveTenure 3인자 호출**

`student-detail.js`의 `fillTenure`(약 100-117) 안에서 history_logs 쿼리와 `deriveTenure` 호출 부분을 아래로 **교체**한다.

```js
        const q = query(
            collection(db, 'history_logs'),
            where('doc_id', '==', studentId),
            orderBy('timestamp', 'desc'),
            limit(200)
        );
        const aq = query(collection(db, 'daily_records'), where('student_id', '==', studentId));
        const [snap, asnap] = await Promise.all([getDocs(q), getDocs(aq)]);
        if (state.selectedStudentId !== studentId) return; // 그 사이 다른 학생 선택됨
        const logs = [];
        snap.forEach(d => logs.push({ id: d.id, ...d.data() }));
        const attendances = [];
        asnap.forEach(d => { const r = d.data(); attendances.push({ date: r.date, status: r.attendance?.status }); });
        const { start, end, startEvent } = deriveTenure(
            logs,
            (l) => l.timestamp?.toDate ? l.timestamp.toDate() : (l.timestamp ? new Date(l.timestamp) : null),
            attendances
        );
        el.textContent = formatTenure(start, end, startEvent, student);
```

- [ ] **Step 3: `formatTenure` 시그니처에 `startEvent` 추가 + 등원예정 분기**

`student-detail.js:82`의 `formatTenure`를 수정한다. 시그니처를
`function formatTenure(start, end, startEvent, student) {` 로 바꾸고,
맨 앞의 `if (!start) return '—';` 한 줄을 다음으로 교체한다:

```js
function formatTenure(start, end, startEvent, student) {
    if (!start) return startEvent ? '등원예정' : '—';
    const startStr = _fmtTenureDate(start);
```
> 나머지 본문(`_fmtTenureDate(start)` 이하)은 그대로 둔다.

- [ ] **Step 4: 빌드 검증**

Run: `cd ~/projects/impact7newDSC && npm run build`
Expected: 빌드 성공.

- [ ] **Step 5: 커밋**

```bash
cd ~/projects/impact7newDSC
git add package.json package-lock.json student-detail.js
git commit -m "feat: 재원기간 기산일을 첫 출석일로 (@impact7/shared v1.11.0)"
```

---

## Task 4: 검증 + 배포

**Files:** 없음 (수동 검증·배포)

- [ ] **Step 1: 임채윤 검증 (미등원 → 등원예정)**

newDSC 또는 DB앱에서 임채윤(선화예/고2, `임채윤_1091083878`) 학생 상세를 연다.
Expected: 헤더 재원기간이 **`'등원예정'`** 으로 표시(이전엔 N일/등록일~현재였음).

- [ ] **Step 2: 정상 재원생 검증**

출석 이력이 있는 임의의 재원생 상세를 연다.
Expected: 재원기간이 등록일이 아닌 **첫 출석일 ~ 현재**로 표시.

- [ ] **Step 3: 재등원생 검증 (있으면)**

퇴원 후 재등원한 학생이 있으면 상세를 연다.
Expected: 마지막 재등원 이후 첫 출석일부터 표시.

- [ ] **Step 4: 배포 (impact7DB 조율)**

```bash
cd ~/projects/impact7DB && git push      # Actions 자동 배포
cd ~/projects/impact7newDSC && git push   # Actions 자동 배포
```
Expected: 양 앱 GitHub Actions 배포 성공.
배포 후 웹에서 Step 1~3 재확인(강력 새로고침).

---

## 비고

- `daily_records`는 `student_id` 단일 `where`만 쓰므로 추가 복합 인덱스 불필요(date range 미사용).
- 양 앱 `attendance.js`/기존 `isAttendedStatus`(채점 가능 여부 등 다른 용도)는 변경하지 않는다. shared의 `isAttendedStatus`는 deriveTenure 내부 필터 전용 SSoT.
- 변경은 표시 전용(파생값)이라 Firestore 데이터·rules 변경 없음.
