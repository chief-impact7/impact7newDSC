# 로그북 학년 필터 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 로그북 소속·반 필터 사이에 초/중/고 학년 다중 선택 필터를 추가해, 선택한 학년의 학생만 일반 그룹에 표시한다(진단평가 제외).

**Architecture:** `GradeFilter.jsx` 컴포넌트가 버튼 3개 + 드롭다운 + 외부클릭 닫기를 캡슐화한다. `App.jsx`가 `gradeFilter`(Set) 상태를 들고 `DailyLogBoard`에 전달하면, `buildLogData`가 일반 학생 루프에서만 학년 키로 필터한다.

**Tech Stack:** React(JSX, 함수형 컴포넌트 + hooks), Vite 빌드, dashboard.css.

**테스트 전략:** 이 프로젝트는 React 컴포넌트 테스트 인프라가 없다(`node --test`로 일부 순수 로직 .test.js만 실행). 따라서 각 Task는 **`npm run build` 통과 + 수동 시나리오 검증**으로 확인한다.

**선행 메모:** 작업 트리에 `export-report.js`의 미커밋 변경(출결사유 컬럼 삭제)이 있다. 본 작업과 무관한 파일이므로, 각 커밋은 **해당 파일만 `git add`**하여 섞이지 않게 한다.

---

## 파일 구조

| 파일 | 책임 | 작업 |
|------|------|------|
| `src/dashboard/components/GradeFilter.jsx` | 초/중/고 버튼 + 드롭다운 + 외부클릭 닫기 | Create |
| `src/dashboard/dashboard.css` | 학년 버튼/드롭다운/active 스타일 | Modify |
| `src/dashboard/App.jsx` | `gradeFilter` 상태 + GradeFilter 렌더 + DailyLogBoard prop | Modify |
| `src/dashboard/components/DailyLogBoard.jsx` | `buildLogData` 학년 필터(일반 그룹만) | Modify |

---

## Task 1: GradeFilter 컴포넌트 + 스타일

**Files:**
- Create: `src/dashboard/components/GradeFilter.jsx`
- Modify: `src/dashboard/dashboard.css`

- [ ] **Step 1: GradeFilter.jsx 생성**

```jsx
import React, { useState, useRef, useEffect } from 'react';

const GRADE_GROUPS = [
    { level: '초등', short: '초', grades: ['4', '5', '6'] },
    { level: '중등', short: '중', grades: ['1', '2', '3'] },
    { level: '고등', short: '고', grades: ['1', '2', '3'] },
];

// value: Set<string> (예: {'초6','중1'}), onChange: (Set) => void
export default function GradeFilter({ value, onChange }) {
    const [openLevel, setOpenLevel] = useState(null);
    const ref = useRef(null);

    useEffect(() => {
        if (!openLevel) return undefined;
        const onDown = (e) => {
            if (ref.current && !ref.current.contains(e.target)) setOpenLevel(null);
        };
        document.addEventListener('mousedown', onDown);
        return () => document.removeEventListener('mousedown', onDown);
    }, [openLevel]);

    const toggleGrade = (key) => {
        const next = new Set(value);
        if (next.has(key)) next.delete(key);
        else next.add(key);
        onChange(next);
    };

    return (
        <div className="dash-grade-filter" ref={ref}>
            {GRADE_GROUPS.map(({ level, short, grades }) => {
                const active = grades.some(g => value.has(short + g));
                return (
                    <div key={level} className="dash-grade-group">
                        <button
                            type="button"
                            className={`dash-grade-btn${active ? ' active' : ''}`}
                            onClick={() => setOpenLevel(openLevel === level ? null : level)}
                        >
                            {level}
                        </button>
                        {openLevel === level && (
                            <div className="dash-grade-dropdown">
                                {grades.map(g => {
                                    const key = short + g;
                                    return (
                                        <label key={key} className="dash-grade-option">
                                            <input
                                                type="checkbox"
                                                checked={value.has(key)}
                                                onChange={() => toggleGrade(key)}
                                            />
                                            {key}
                                        </label>
                                    );
                                })}
                            </div>
                        )}
                    </div>
                );
            })}
        </div>
    );
}
```

- [ ] **Step 2: dashboard.css에 스타일 추가**

`dashboard.css` 끝에 추가한다(기존 `.dash-filter-group` 패턴과 어울리는 컴팩트 스타일).

```css
/* ─── 학년 필터 ─── */
.dash-grade-filter {
    display: flex;
    gap: 4px;
}
.dash-grade-group {
    position: relative;
}
.dash-grade-btn {
    padding: 5px 10px;
    font-size: 13px;
    border: 1px solid var(--border, #dadce0);
    border-radius: 6px;
    background: #fff;
    color: var(--text-sec, #5f6368);
    cursor: pointer;
}
.dash-grade-btn.active {
    background: var(--primary, #4285f4);
    border-color: var(--primary, #4285f4);
    color: #fff;
}
.dash-grade-dropdown {
    position: absolute;
    top: calc(100% + 4px);
    left: 0;
    z-index: 50;
    background: #fff;
    border: 1px solid var(--border, #dadce0);
    border-radius: 6px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
    padding: 6px;
    display: flex;
    flex-direction: column;
    gap: 2px;
    min-width: 72px;
}
.dash-grade-option {
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 13px;
    padding: 4px 6px;
    border-radius: 4px;
    cursor: pointer;
    white-space: nowrap;
}
.dash-grade-option:hover {
    background: var(--surface, #f1f3f4);
}
```

- [ ] **Step 3: 빌드 검증**

Run: `npm run build`
Expected: 빌드 성공(에러 없음). GradeFilter는 아직 어디서도 import되지 않아 번들에 미포함될 수 있으나, 구문 오류는 잡힌다.

- [ ] **Step 4: 커밋**

```bash
git add src/dashboard/components/GradeFilter.jsx src/dashboard/dashboard.css
git commit -m "feat: 로그북 학년 필터 컴포넌트(GradeFilter) + 스타일"
```

---

## Task 2: App.jsx 연결 (상태 + 렌더 + prop 전달)

**Files:**
- Modify: `src/dashboard/App.jsx`

- [ ] **Step 1: GradeFilter import 추가**

`App.jsx` 상단 import 블록에 추가(다른 컴포넌트 import 옆):
```jsx
import GradeFilter from './components/GradeFilter';
```

- [ ] **Step 2: gradeFilter 상태 추가**

`App.jsx`의 `const [classFilter, setClassFilter] = useState('');`(52행) 바로 다음 줄에 추가:
```jsx
    const [gradeFilter, setGradeFilter] = useState(new Set());
```

- [ ] **Step 3: 소속과 반 사이에 GradeFilter 렌더**

소속 `dash-filter-group`의 닫는 `</div>`(301행)와 반 `dash-filter-group`(303행) 사이에 삽입:
```jsx
                <div className="dash-filter-group">
                    <label>학년</label>
                    <GradeFilter value={gradeFilter} onChange={setGradeFilter} />
                </div>
```

- [ ] **Step 4: DailyLogBoard에 gradeFilter prop 전달**

`<DailyLogBoard ... classFilter={classFilter}` 다음 줄(327행 부근)에 추가:
```jsx
                            gradeFilter={gradeFilter}
```
결과:
```jsx
                        <DailyLogBoard
                            students={students}
                            dailyLog={dailyLog}
                            branchFilter={branchFilter}
                            classFilter={classFilter}
                            gradeFilter={gradeFilter}
                            date={baseDate}
                        />
```

- [ ] **Step 5: 빌드 검증**

Run: `npm run build`
Expected: 빌드 성공. 이 시점에 버튼/드롭다운은 화면에 보이지만 아직 필터는 작동 안 함(DailyLogBoard 미적용).

- [ ] **Step 6: 커밋**

```bash
git add src/dashboard/App.jsx
git commit -m "feat: App에 gradeFilter 상태 + GradeFilter 렌더(소속/반 사이)"
```

---

## Task 3: DailyLogBoard 학년 필터 적용 (일반 그룹만)

**Files:**
- Modify: `src/dashboard/components/DailyLogBoard.jsx`

- [ ] **Step 1: buildLogData 시그니처에 gradeFilter 추가**

`function buildLogData({ students, dailyLog, branchFilter, classFilter, date }) {`(204행)을 다음으로 변경:
```jsx
function buildLogData({ students, dailyLog, branchFilter, classFilter, gradeFilter, date }) {
```

- [ ] **Step 2: 학년 키 헬퍼 + 일반 학생 루프 필터 추가**

`buildLogData` 내부의 일반 학생 루프 시작부 `students.forEach(student => {`(약 272행) 직후, 기존 `if (branchFilter && getBranch(student) !== branchFilter) return;`(약 275행) **다음 줄**에 학년 필터를 추가한다. 먼저 헬퍼를 같은 함수 스코프 상단(레코드 맵 만드는 줄들 근처, 약 246행 `const dayName = getDayName(date);` 다음)에 정의:

```jsx
    const LEVEL_SHORT = { '초등': '초', '중등': '중', '고등': '고' };
    const gradeKey = (s) => (LEVEL_SHORT[s.level] || '') + (s.grade != null ? s.grade : '');
```

그리고 일반 학생 루프 안, `branchFilter` 가드 다음에:
```jsx
        if (gradeFilter?.size && !gradeFilter.has(gradeKey(student))) return;
```

> 진단평가(`tempAttendances` 매핑, 약 225행)에는 추가하지 않는다 — 항상 전체 표시.

- [ ] **Step 3: DailyLogBoard props + useMemo 연결**

`export default function DailyLogBoard({ students, dailyLog, branchFilter, classFilter, date }) {`(530행)을 변경:
```jsx
export default function DailyLogBoard({ students, dailyLog, branchFilter, classFilter, gradeFilter, date }) {
```

useMemo 호출(532행)을 변경:
```jsx
        buildLogData({ students, dailyLog, branchFilter, classFilter, gradeFilter, date }),
```

같은 useMemo의 의존성 배열에 `gradeFilter`를 추가한다. 현재 배열을 찾아(`[students, dailyLog, branchFilter, classFilter, date]`) 다음으로:
```jsx
    [students, dailyLog, branchFilter, classFilter, gradeFilter, date]);
```

- [ ] **Step 4: 빌드 검증**

Run: `npm run build`
Expected: 빌드 성공.

- [ ] **Step 5: 커밋**

```bash
git add src/dashboard/components/DailyLogBoard.jsx
git commit -m "feat: 로그북 학년 필터 적용(일반 그룹만, 진단평가 제외)"
```

---

## Task 4: 수동 검증 + 배포

**Files:** 없음

- [ ] **Step 1: dev 서버로 수동 검증**

Run: `npm run dev` (포트 5174). 로그북 화면을 연다.
확인 시나리오:
1. 소속과 반 사이에 **초등/중등/고등 버튼 3개**가 보인다
2. **초등** 클릭 → 드롭다운에 **초4·초5·초6** 체크박스. **중등** → 중1·중2·중3, **고등** → 고1·고2·고3
3. **초6** 체크 → 즉시 일반 그룹(정규/내신 등)에 초6 학생만 표시, **진단평가 섹션은 그대로 전체**
4. 초6 + 중1 체크 → 두 학년 모두 표시(OR)
5. 소속 2단지 + 초6 → 2단지의 초6만(AND)
6. 선택된 학년이 있으면 해당 버튼이 **색 강조(active)**
7. 전체 해제 → 전체 표시
8. 드롭다운 열고 바깥 클릭 → 닫힘. 초등 열린 상태에서 중등 클릭 → 초등 닫히고 중등 열림

- [ ] **Step 2: 배포**

```bash
git push   # GitHub Actions 자동 배포
```
Expected: Actions 배포 성공. 웹에서 강력 새로고침 후 Step 1 시나리오 재확인.

---

## 비고

- `gradeFilter`는 `Set`이라 `useMemo` 의존성 비교가 참조 동등으로 동작한다. `setGradeFilter(new Set(...))`로 항상 새 Set을 만들어 넘기므로(GradeFilter의 `toggleGrade`) 리렌더가 정상 트리거된다.
- 학년 키는 옵션(`short + g`, 예 `'초6'`)과 학생(`gradeKey`, 예 `'초'+6`)이 동일 규칙이라 매칭된다. `student.grade`가 숫자든 문자열이든 문자열 연결 결과가 같다.
